<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data da publicação: {{postagem.data}}</small>
        <br>
        <h2>{{postagem.descricao}}</h2>
        <p>{{postagem.conteudo}}</p>
    </div>
</div>